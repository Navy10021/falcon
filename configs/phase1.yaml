# Phase 1: Uncertainty-Aware GNN + Blue PPO 훈련 설정

training:
  episodes: 500
  lr: 3.0e-4
  seed: 42
  mc_samples: 10
  ppo_epochs: 4
  log_interval: 50
  save_interval: 200
  checkpoint_dir: "checkpoints"
